#include "main.h"
#include <stdio.h>

/**
 *  _abs(int) - return the absolute value of the number
 * @n:
 *
 * Return: Absolute number
 *
 */
int _abs(int)
{
	int nbr;

	if (nbr < 0)
	{
		nbr = -nbr;
	}
		printf("%d\n", nbr);

	else
		printf("%d\n", nbr)


	return (0);
}
