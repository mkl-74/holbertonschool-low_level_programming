#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	char lowReverse;

	for (lowReverse = 'z'; lowReverse >= 'a'; lowReverse--)
		putchar (lowReverse);
{
	putchar ('\n');
}

	return (0);
}
